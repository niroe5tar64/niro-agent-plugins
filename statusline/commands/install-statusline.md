---
description: statusline をプロジェクトの settings.local.json に設定する
allowed-tools: Bash(bash:*), Glob(*)
---

Glob で `**/statusline/scripts/initialize-settings.sh` を探し、
見つかったスクリプトを bash で実行してください。
実行結果（stdout/stderr）をそのまま報告してください。
