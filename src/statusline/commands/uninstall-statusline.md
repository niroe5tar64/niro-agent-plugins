---
description: statusline の設定を settings.local.json から削除する
allowed-tools: Bash(bash:*), Glob(*)
---

Glob で `**/statusline/scripts/uninstall-statusline.sh` を探し、
見つかったスクリプトを bash で実行してください。
実行結果（stdout/stderr）をそのまま報告してください。
